import { useState } from "react";
import { Routes, Route, Link } from "react-router-dom";
import "../index.css";

const NoPage = () => {
  return (
    <div>
      <h1>Sorry! We're not loading right now.</h1>
    </div>
  );
};

export default NoPage;
